import React from 'react';

const HostColumn = () => {
  return <div className="column">
    <p>No user has claimed host yet</p>
    <button>claim host?</button>
  </div>
}

export default HostColumn;
